<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.ralfspoeth</groupId>
        <artifactId>plumbum</artifactId>
        <version>2.0.8</version>
    </parent>

    <properties>
        <maven.compiler.release>26</maven.compiler.release>
        <revision>0.0.3</revision>
    </properties>

    <artifactId>json-rpc</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>

    <modules>
        <module>greyson-rpc</module>
        <module>rpc-servlet</module>
        <module>rpc-greylet</module>
    </modules>

    <name>json-rpc</name>
    <description>
        JSON-RPC 2.0 for Java: a transport-independent, Greyson-native
        processor (greyson-rpc) and servlet/websocket adapters with a
        Greyson-free API (rpc-servlet) or a deliberately Greyson-native
        one (rpc-greylet).
    </description>
    <url>https://github.com/ralfspoeth/json-rpc</url>

    <scm>
        <developerConnection>scm:git:https://github.com/ralfspoeth/json-rpc.git</developerConnection>
        <url>scm:git:https://github.com/ralfspoeth/json-rpc</url>
        <tag>json-rpc-0.0.3</tag>
    </scm>

    <build>
        <plugins>
            <plugin>
                <groupId>org.sonatype.plugins</groupId>
                <artifactId>nexus-staging-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-gpg-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <compilerArgs>
                        <arg>--enable-preview</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <redirectTestOutputToFile>true</redirectTestOutputToFile>
                    <!--suppress UnresolvedMavenProperty -->
                    <argLine>--enable-preview -javaagent:${org.mockito:mockito-core:jar}</argLine>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <configuration>
                    <additionalOptions>--enable-preview</additionalOptions>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>properties</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>6.1.2</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>jakarta.platform</groupId>
                <artifactId>jakarta.jakartaee-bom</artifactId>
                <version>11.0.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-bom</artifactId>
                <version>5.23.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Greyson IO -->
            <dependency>
                <groupId>io.github.ralfspoeth</groupId>
                <artifactId>json</artifactId>
                <version>1.6.1</version>
            </dependency>
            <dependency>
                <groupId>io.github.ralfspoeth</groupId>
                <artifactId>utf8</artifactId>
                <version>1.0.4</version>
            </dependency>
            <dependency>
                <groupId>io.github.ralfspoeth</groupId>
                <artifactId>basix</artifactId>
                <version>1.5.4</version>
            </dependency>
            <!-- jetty -->
            <dependency>
                <groupId>org.eclipse.jetty.ee10</groupId>
                <artifactId>jetty-ee10-servlet</artifactId>
                <version>12.1.11</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>
